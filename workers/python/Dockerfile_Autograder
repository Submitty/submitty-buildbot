FROM submitty-python-worker:latest

ENV COVERAGE_FILE /buildbot/python-autograder-tests/build/autograder/.coverage.autograder

USER buildbot
WORKDIR /buildbot

CMD ["/usr/bin/dumb-init", "twistd", "--pidfile=", "-ny", "buildbot.tac"]